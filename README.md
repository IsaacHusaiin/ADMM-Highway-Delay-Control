# ADMM-MPC Ramp Metering on I-405 SB

A cell-transmission-model (CTM) study of coordinated on-ramp metering for the I-405
southbound corridor, using real Caltrans PeMS detector data. An ADMM-based model-predictive
controller (ADMM-MPC) is compared against three references on an identical corridor, demand
scenario, and objective:

- **Baseline** — no control (observed PeMS ramp releases)
- **Greedy ADMM** — single-step (myopic) ADMM, no prediction horizon
- **Fixed policy** — release a constant fraction of the observed rate (ablation grid)
- **ADMM-MPC** — receding-horizon ADMM with a K-step lookahead

---

## Corridor and data

- **Freeway:** I-405 South, absolute postmile **8.17 → 13.07** (≈ 4.90 mi)
- **Detectors:** 7 mainline + 5 on-ramps + 3 off-ramps (RED HILL, BRISTOL 1, FAIRVIEW,
  HARBOR 1, HARBOR 2, EUCLID, TALBERT)
- **Discretization:** 8 equal CTM cells (0.6125 mi each), Δt = 30 s, 120 steps = 08:00–09:00
- **Data source:** PeMS 5-minute station data, District 12
  - `station_5min_district12.txt` — timestamped flow / occupancy / speed
  - `Station Metadata_district12.txt` — detector IDs, postmiles, lanes
- **Derived inputs:** 5-min flows are split into 30-s values (÷10); free-flow speeds are the
  median speed over the 01:00–05:00 low-congestion window; ramp arrivals are modeled as
  `1.5 × observed release` (held identical across all controllers).

---

## Model

**CTM update (per cell, per 30 s):**
sending = min(x, doorway capacity); receiving = min(doorway, physical − x);
`x_next = x + q_in + u_ramp − q_out − f_offramp` (floored at 0).

**Mainline delay (clamped, non-negative):**
`D = max( mean(x)·Δt − (q_out + f)·TT_ff , 0 )` per cell, summed over cells and steps.

**Objective (normalized, weighted):** each term is divided by its baseline value so the
terms are comparable, then weighted:

```
J = alpha_main · (mainline_delay / D_main_base)
  + beta_local · (local_delay   / D_local_base)
  + gamma      · (fairness      / L_fair_base)
  + lambda_1   · (doorway_pen   / P_door_base)
  + lambda_2   · (safe_pen      / P_safe_base)
  + lambda_3   · (physical_pen  / P_phys_base)
  + lambda_4   · (spillback_pen / P_spill_base)
```

Weights: `alpha_main=30, beta_local=1, gamma=1, lambda_1=1, lambda_2=0.5, lambda_3=1,
lambda_4=0.5`. By construction the baseline normalizes to **J = 34.0**.

---

## Repository structure

| File | Role |
|---|---|
| `Benchmark_calculation.ipynb` | Builds the corridor, CTM, capacities, and the no-control baseline. Exports `shared_benchmark_inputs.pkl`. **Run this first.** |
| `ADMM_MPC.ipynb` | Receding-horizon ADMM-MPC controller and its 120-step simulation. |
| `Greedy_ADMM.ipynb` | Single-step (myopic) ADMM controller. |
| `fixed_policy.ipynb` | Fixed-rate metering ablation grid + served-vehicle accounting + three-way comparison. |
| `station_5min_district12.txt` | PeMS 5-minute detector data. |
| `Station Metadata_district12.txt` | PeMS detector metadata. |
| `shared_benchmark_inputs.pkl` | Generated by the benchmark; carries inputs/denominators to all controllers. |

### How consistency is enforced
Every controller notebook starts with `%run Benchmark_calculation.ipynb` (to inherit the exact
CTM and clamped-delay functions) and then loads `shared_benchmark_inputs.pkl` and asserts that
its inputs, capacities, weights, and baseline denominators match the benchmark. This guarantees
all policies are scored on the same model and the same scale.

---

## How to run

Order matters — the controllers depend on the benchmark's pickle.

1. Put the two `.txt` data files in the working directory.
2. Run **`Benchmark_calculation.ipynb`** (Restart & Run All). It writes
   `shared_benchmark_inputs.pkl`.
3. Run any controller notebook (Restart & Run All):
   - `ADMM_MPC.ipynb`
   - `Greedy_ADMM.ipynb`
   - `fixed_policy.ipynb` (this one `%run`s both the benchmark and ADMM-MPC, so it reproduces
     the full comparison).

**Requirements:** Python 3.x, `numpy`, `pandas`, `scipy` (SLSQP for the ADMM block updates),
`matplotlib`. Runtime is dominated by ADMM-MPC (a few minutes for the 120-step run).

---

## Configuration reference

| Parameter | Value |
|---|---|
| Corridor | I-405 S, PM 8.17–13.07 |
| CTM cells | 8 (0.6125 mi) |
| Timestep / horizon | 30 s / 120 steps (1 h) |
| Jam density | 193 veh/mi/lane |
| Free-flow window | 01:00–05:00 (median speed) |
| Ramp arrival model | 1.5 × observed release |
| Meter capacity | observed release (controllers meter down only) |
| Objective weights | alpha=30, beta=1, gamma=1, lambda=1/0.5/1/0.5 |
| ADMM-MPC horizon K | 5 |
| ADMM | rho=0.2, min/max iters 20/150, tol_primal 1e-3, tol_dual 1e-2 |
