

# ADMM-MPC Ramp Metering with a 9-Cell CTM

This project studies freeway ramp metering using a 9-cell Cell Transmission Model (CTM), Caltrans PeMS District 5 data, a fixed-policy baseline, a centralized convex QP surrogate, and an ADMM-MPC controller.

The goal is to test whether an optimized controller can reduce the total system objective compared with historical ramp-release behavior and simple fixed metering policies.

---

## Main idea

The project compares four policies/models:

| File                          | Purpose                                                                                                |
| ----------------------------- | ------------------------------------------------------------------------------------------------------ |
| `Benchmark_calculation.ipynb` | Builds the exact CTM benchmark and exports shared inputs.                                              |
| `fixed_policy.ipynb`          | Tests simple fixed release scales, such as `1.1 × observed_release`.                                   |
| `Benchmark_convex_QP.ipynb`   | Builds the centralized convex QP surrogate used as the optimization reference.                         |
| `ADMM_MPC.ipynb`              | Final receding-horizon controller; optimizes, applies one command, updates the exact CTM, and replans. |

All notebooks use:

```text
shared_benchmark_inputs.pkl
```

which is generated by `Benchmark_calculation.ipynb`.

---

## Data

Raw PeMS inputs:

```text
d05_text_meta_2026_04_28.txt
d05_text_station_5min_2026_05_27.txt
```

The benchmark notebook converts the 5-minute PeMS detector data into 15-second CTM inputs.

---

## CTM setup

Final model setup:

| Item               |         Value |
| ------------------ | ------------: |
| Mainline cells     |             9 |
| Controlled ramps   |             4 |
| Time step          |    15 seconds |
| Horizon            |     480 steps |
| Window             |       2 hours |
| Arrival multiplier |           1.2 |
| Cell 9 capacity    | 2400 veh/hour |
| Spillback penalty  |        Linear |

Ramp-to-cell mapping:

```text
4th    → Cell 2
Price  → Cell 2
Mattie → Cell 6
Avila  → Cell 9
```

Avila uses a priority merge into Cell 9 with priority:

```text
π = 0.3
```

---

## Objective

The raw objective is:

```text
mainline delay
+ ramp delay
+ fairness penalty
+ doorway penalty
+ safe-threshold penalty
+ physical-capacity penalty
+ spillback penalty
```

Fairness uses physical ramp queue stress:

```text
φ = clip(R / Rmax, 0, 1)
```

Spillback is handled separately and penalized linearly:

```text
spillback penalty = λ₄ · B
```

---

## Model roles

### Benchmark

`Benchmark_calculation.ipynb` replays the historical observed ramp releases through the exact CTM.

It is the source-of-truth baseline and evaluator.

Final benchmark raw objective:

```text
130,423
```

---

### Fixed policy

`fixed_policy.ipynb` tests simple scaled release commands:

```text
u_fixed = s × observed_release
```

Best fixed policy:

```text
release scale = 1.10
raw objective = 83,436
```

This is the simple non-optimization baseline.

---

### Convex QP

`Benchmark_convex_QP.ipynb` builds a convex surrogate of the exact CTM control problem.

The exact CTM has nonconvex pieces such as min/max flow rules, merge switching, and queue/spillback splitting. The QP replaces these with convex capacity constraints and epigraph/slack variables.

Important:

```text
The convex QP is not the exact benchmark replay.
It is a centralized convex reference model.
```

Its internal QP optimum is about:

```text
52,637
```

but replaying its commands in the exact CTM gives a much higher value, so it is used as an optimization reference, not as the final controller result.

---

### ADMM-MPC

`ADMM_MPC.ipynb` is the final controller.

At every 15-second step, it:

```text
observes the current exact CTM state
solves a short-horizon convex QP
applies only the first optimized ramp command
updates the exact CTM
replans
```

Final controller setting:

```text
K = 10
AW = 2.4
METER = 1.2
```

This gives the best total raw objective among the tested policies.

---

## Final results

Exact-CTM realized raw objectives:

| Policy                      | Raw objective |
| --------------------------- | ------------: |
| Historical benchmark        |       130,423 |
| Best fixed policy           |        83,436 |
| Open-loop full-horizon plan |        73,415 |
| ADMM-MPC                    |    **61,218** |

Approximate improvement from ADMM-MPC:

| Comparison           | Improvement |
| -------------------- | ----------: |
| vs benchmark         | 53.1% lower |
| vs best fixed policy | 26.6% lower |
| vs open-loop         | 16.6% lower |

---

## How to run

Run notebooks in this order:

```text
1. Benchmark_calculation.ipynb
2. fixed_policy.ipynb
3. Benchmark_convex_QP.ipynb
4. ADMM_MPC.ipynb
```

The benchmark must run first because it creates:

```text
shared_benchmark_inputs.pkl
```

---

## Requirements

```text
numpy
pandas
matplotlib
cvxpy
clarabel
```

Optional:

```text
scipy
```

---

## Important note

ADMM-MPC should not be described as better in every metric.

The correct claim is:

```text
ADMM-MPC achieves the best total raw objective among the tested policies.
```

It manages a trade-off between freeway delay, ramp delay, spillback, fairness, and capacity penalties.

The convex QP and ADMM convergence claims apply to the convex surrogate problem. Final performance is always evaluated using the exact CTM replay.
